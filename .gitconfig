[alias]
  br = branch
  co = checkout
  amend = commit --amend --no-edit
  lv = log --graph --oneline --decorate --color --all
  assume = update-index --assume-unchanged
  unassume = update-index --no-assume-unchanged
[pager]
  branch = false
  diff = false
